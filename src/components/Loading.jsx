import React from "react";
import Cloud from "../assets/Cloud.svg";

const Loading = () => {
   return (
      <>
         <img src={Cloud} alt="Loding..." className="pop-up" />
      </>
   );
};

export default Loading;
